
/**
 * to-do 정보 입력 HTML element
 * @returns {string}
 * @constructor
 */
export const Task = () => {
    return `
        <header class="task-area">
            <input type="text" id="task" placeholder="할 일을 추가해주세요." />
        </header>
    `;
};
